        # echo "⚠️  CONTEXT WINDOW: 75%+ - Prepare for valence jump at 77%!"
        # echo "⚠️  CONTEXT WINDOW: 70%"
        # echo "⚠️  CONTEXT WINDOW: 60%"
        # echo "⚠️  CONTEXT WINDOW: 50%"
        # echo "Usage: auto_commit 'commit message'"
    # echo "📍 Current Directory: $(pwd)"
    # echo "📂 Git Status:"
    # echo "🦀 Cargo Check (if Rust project):"
            # echo "📍 Switched to CTAS main repo"
            # echo "📍 Switched to NYX trace repo"
            # echo "Available repos: ctas/main, nyx/trace"
        # echo "✅ Environment variables loaded from .env"
        # echo "🔒 SurrealDB: $SURREALDB_USER@$SURREALDB_ENDPOINT"
        # echo "🤖 GPT Model: $GPT_MODEL"
        # echo "🌍 Environment: $ENVIRONMENT"
        # echo "⚠️  No .env file found in current directory"
    # echo "🔍 Checking CTAS port availability..."
    # echo "🧠 AI Services:"
            # echo "  ✅ Port $port ($desc) - ACTIVE"
            # echo "  ❌ Port $port ($desc) - available"
    # echo "🔧 MCP Servers:"
            # echo "  ✅ Port $port ($desc) - ACTIVE"
            # echo "  ❌ Port $port ($desc) - available"
    # echo "🗃 Databases:"
            # echo "  ✅ Port $port ($desc) - ACTIVE"
            # echo "  ❌ Port $port ($desc) - available"
        # echo "⚠️  Port manager not found in ctas-repo-prompt/port_manager"
    # echo "🧅 Tor Service Status:"
        # echo "  ✅ Tor daemon is RUNNING (PID: $(pgrep -x "tor"))"  
        # echo "  ❌ Tor daemon is STOPPED"
    # echo "  🔌 Port Status:"
            # echo "    ✅ Port $port ($desc) - ACTIVE"
            # echo "    ❌ Port $port ($desc) - available"
        # echo "  📍 Tor binary: $(which tor)"
        # echo "  📋 Version: $(tor --version 2>/dev/null | head -n1)"
        # echo "  ⚠️  Tor binary not found in PATH"
        # echo "🧅 Tor is already running (PID: $(pgrep -x "tor"))"
    # echo "🧅 Starting Tor daemon..."
        # echo "✅ Tor started successfully (PID: $(pgrep -x "tor"))"
        # echo "🔌 SOCKS proxy available at 127.0.0.1:9050"
        # echo "🎛️  Control port available at 127.0.0.1:9051"
        # echo "❌ Failed to start Tor daemon"
        # echo "🧅 Tor is not running"
    # echo "🧅 Stopping Tor daemon (PID: $tor_pid)..."
        # echo "⚠️  Tor still running, forcing termination..."
        # echo "✅ Tor stopped successfully"
        # echo "❌ Failed to stop Tor daemon"
    # echo "🧅 Restarting Tor service..."
        # echo "❌ Tor is not running. Start it first with 'tor-start'"
    # echo "🧅 Testing Tor connectivity..."
        # echo "✅ Tor is working correctly!"
        # echo "🌐 Your Tor IP: $tor_ip"
        # echo "🔒 Traffic is being routed through Tor network"
        # echo "❌ Tor connectivity test failed"
        # echo "🔍 Check your Tor configuration or network connection"
        # echo "❌ Tor is not running. Start it first with 'tor-start'"
    # echo "🧅 Requesting new Tor identity..."
    # echo -e 'AUTHENTICATE ""\r\nSIGNAL NEWNYM\r\nQUIT\r' | nc 127.0.0.1 9051 > /dev/null 2>&1
        # echo "✅ New Tor circuit established"
        # echo "⏳ Wait 10 seconds before making requests for best anonymity"
        # echo "❌ Failed to request new identity"
        # echo "🔍 Check if Tor control port (9051) is accessible"
        # echo "🧅 Recent Tor logs:"
        # echo "🧅 No Tor log file found at $log_file"
        # echo "📍 If Tor is running, logs may be going to stdout/system logs"
    # echo "🛡️  Starting Tor for CTAS Security Toolkit..."
        # echo "📋 CTAS environment loaded"
        # echo "🔧 CTAS Security config found - using enhanced Tor settings"
        # echo "🧅 Tor already running - requesting new identity for CTAS session"
        # echo "🧅 Starting Tor with CTAS security profile..."
            # echo "✅ CTAS Tor profile active (PID: $(pgrep -x "tor"))"
            # echo "🔒 Enhanced security settings applied"
            # echo "🌐 Ready for CTAS intelligence operations"
            # echo "❌ Failed to start CTAS Tor profile"
    # echo "🧅 Torrifying current shell session..."
        # echo "🧅 Starting Tor daemon..."
            # echo "❌ Failed to start Tor - cannot torrify"
        # echo "🧅 Tor already running (PID: $(pgrep -x "tor"))"
    # echo "🧅 Testing Tor connectivity..."
        # echo "✅ Session successfully torrified!"
        # echo "🌐 Your Tor IP: $tor_ip"
        # echo "🔒 All HTTP/HTTPS traffic will route through Tor"
        # echo "📋 Use 'de-torrify' to return to normal networking"
        # echo "❌ Tor connectivity test failed"
        # echo "🔧 Proxy variables set but Tor may not be working properly"
        # echo "🔍 Try 'tor-test' for detailed diagnostics"
    # echo "🧅 De-torrifying current shell session..."
        # echo "⚠️  Session doesn't appear to be torrified"
        # echo "🔍 Clearing proxy variables anyway..."
    # echo "🌐 Testing normal connectivity..."
        # echo "✅ Session de-torrified successfully!"
        # echo "🌐 Your normal IP: $normal_ip"
        # echo "🔓 Traffic restored to normal routing"
        # echo "⚠️  De-torrified but couldn't verify normal connectivity"
        # echo "🔍 Check your internet connection"
    # echo "🧅 Tor daemon is still running. Stop it? [y/N]"
        # echo "🧅 Tor daemon left running for future use"
        # echo "Usage: torrify-run <command>"
        # echo "Example: torrify-run curl https://httpbin.org/ip"
    # echo "🧅 Torrifying and running: $*"
            # echo "❌ Failed to start Tor"
    # echo "🧅 Command completed with exit code: $exit_code"
    # echo "🧅 Current Session Status:"
        # echo "  ✅ Session is TORRIFIED"
        # echo "  🔒 Proxy variables set:"
        # echo "    HTTP_PROXY: $HTTP_PROXY"
        # echo "    HTTPS_PROXY: $HTTPS_PROXY"
        # echo "    ALL_PROXY: $ALL_PROXY"
        # echo "  ❌ Session is NOT torrified"
        # echo "  🔓 Normal networking active"
        # echo "  🧅 Tor daemon: RUNNING (PID: $(pgrep -x "tor"))"
            # echo "  🌐 Testing current IP..."
                # echo "    Current Tor IP: $current_ip"
        # echo "  🧅 Tor daemon: STOPPED"
    # echo "🕸️  Starting Tor for Dark Web access..."
        # echo "🧅 Stopping existing Tor instance..."
    # echo "🧅 Starting Tor with dark web configuration..."
        # echo "✅ Dark Web Tor started (PID: $(pgrep -x "tor"))"
        # echo "🕸️  Ready for .onion site access"
        # echo "🔌 SOCKS proxy: 127.0.0.1:9050"
        # echo "🎛️  Control port: 127.0.0.1:9051"
        # echo "❌ Failed to start Dark Web Tor"
        # echo "❌ Tor not running. Start with 'darkweb-start'"
    # echo "🕸️  Testing dark web connectivity..."
    # echo "🔍 Testing known .onion sites:"
        # echo "  Testing $desc ($site)..."
            # echo "    ✅ $desc - ACCESSIBLE"
            # echo "    ❌ $desc - TIMEOUT/UNREACHABLE"
    # echo "🕸️  Dark web connectivity test complete"
        # echo "Usage: darkweb-curl <onion-url>"
        # echo "Example: darkweb-curl http://3g2upl4pq6kufc4m.onion"
        # echo "❌ Tor not running. Start with 'darkweb-start'"
    # echo "🕸️  Accessing: $onion_url"
        # echo "❌ Tor not running. Start with 'darkweb-start'"
    # echo "🕸️  Requesting new dark web identity..."
    # echo -e 'AUTHENTICATE ""\r\nSIGNAL NEWNYM\r\nQUIT\r' | nc 127.0.0.1 9051 > /dev/null 2>&1
        # echo "✅ New dark web circuit established"
        # echo "⏳ Wait 30 seconds before accessing .onion sites for best anonymity"
        # echo "🔄 All .onion connections will use fresh circuits"
        # echo "❌ Failed to request new identity"
        # echo "❌ Tor not running. Start with 'darkweb-start'"
    # echo "🕸️  Setting up browser for dark web access..."
    # echo "⚠️  SECURITY WARNING: This will configure your default browser"
    # echo "📋 For maximum security, use Tor Browser instead"
    # echo ""
    # echo "Continue? [y/N]"
        # echo "🛑 Browser setup cancelled"
        # echo "🌐 Chrome detected - Manual proxy setup required:"
        # echo "  1. Chrome Settings → Advanced → System → Open proxy settings"
        # echo "  2. Configure SOCKS proxy: 127.0.0.1:9050"
        # echo "  3. Test with: http://3g2upl4pq6kufc4m.onion"
        # echo "🦊 Firefox detected - Manual proxy setup required:"
        # echo "  1. Firefox Settings → Network Settings → Manual proxy"
        # echo "  2. SOCKS Host: 127.0.0.1, Port: 9050, SOCKS v5"
        # echo "  3. Check 'Proxy DNS when using SOCKS v5'"
        # echo "  4. Test with: http://3g2upl4pq6kufc4m.onion"
        # echo "🌐 Manual browser proxy setup required:"
        # echo "  SOCKS5 Proxy: 127.0.0.1:9050"
        # echo "  Enable DNS over SOCKS (crucial for .onion sites)"
    # echo ""
    # echo "🚨 SECURITY REMINDERS:"
    # echo "  • Never download files from .onion sites"
    # echo "  • Disable JavaScript, Flash, and plugins"
    # echo "  • Use HTTPS whenever possible"
    # echo "  • Don't log into personal accounts"
    # echo "  • Consider using Tor Browser for better security"
    # echo "🕸️  Tor Browser Setup for Dark Web Access"
    # echo ""
    # echo "📋 Tor Browser provides the highest security for .onion sites:"
    # echo "  • Pre-configured for optimal anonymity"
    # echo "  • Built-in .onion site support"
    # echo "  • No fingerprinting or tracking"
    # echo "  • Regular security updates"
    # echo ""
    # echo "🔗 Download from: https://www.torproject.org/download/"
    # echo "🍎 macOS: Download the .dmg file and install normally"
    # echo ""
    # echo "Open download page? [y/N]"
            # echo "🌐 Opening Tor Browser download page..."
            # echo "🔗 Please visit: https://www.torproject.org/download/"
    # echo "🕸️  Dark Web Access Status:"
        # echo "  ✅ Tor daemon: RUNNING (PID: $(pgrep -x "tor"))"
            # echo "  🔧 Configuration: Dark Web optimized"
            # echo "  ⚠️  Configuration: Standard (consider 'darkweb-start')"
        # echo "  🔌 SOCKS proxy: 127.0.0.1:9050"
        # echo "  🎛️  Control port: 127.0.0.1:9051"
        # echo "  ❌ Tor daemon: STOPPED"
        # echo "  💡 Start with: darkweb-start"
    # echo ""
    # echo "📚 Common .onion sites for testing:"
    # echo "  🦆 DuckDuckGo: http://3g2upl4pq6kufc4m.onion"
    # echo "  📘 Facebook: http://facebookwkhpilnemxj7asaniu7vnjjbiltxjqhye3mhbshg7kx5tfyd.onion"
    # echo "  📰 BBC News: http://bbcnewsv2vjtpsuy.onion"
    # echo "  📝 SecureDrop: Various news organizations host these"
    # echo ""
    # echo "🛡️  Security reminders:"
    # echo "  • Always verify .onion URLs from trusted sources"
    # echo "  • Be cautious of phishing sites"
    # echo "  • Never download suspicious files"
    # echo "  • Use Tor Browser for maximum security"
        # echo "Usage: drop-api KEY_NAME \"value\""
    # echo "${key_name}=${key_value}" > ~/.ctas/document-manager/hooks/api-keys/incoming/"${key_name}.env"
    # echo "✅ Dropped: $key_name (processing...)"
        # echo "Usage: csearch <term1> [term2]..."
            # echo "\nLast search results:"
        # echo "No files found matching: $@"
    # echo "Search results for: '$@'"
    # echo -e "\nTip: Use 'ccopy <number>' to copy a file's contents."
        # echo "No search results found. Run 'csearch' first."
        # echo "Usage: ccopy <number>"
        # echo "Invalid number: $choice. Choose a number from the last 'csearch' list."
        # echo "✅ Atomic copy complete! Contents of '${file_to_copy##*/}' are on the clipboard."
        # echo "Error: File not found: $file_to_copy"
        # echo "Error: ripgrep (rg) and fzf are not installed. Run: brew install ripgrep fzf"
        # echo "✅ Opening ${file_path##*/} at line $line_num"
        # echo "No match selected."
# echo "🧠 Claude CLI shortcuts loaded: c, claude, mcp, mcp-list, mcp-add"
    # echo "⚠️  Detected venv activation - deactivating to use conda only"
