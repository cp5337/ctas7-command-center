# Example .env for CTAS7 orchestration
# Copy to .env and fill with real secrets. DO NOT commit .env to git.

# Enable Synaptix auto-triggering
SYNAPTIX_ENABLED=false

# Zen Coder token (use file-based storage preferred)
ZEN_TOKEN=""

# GitHub Personal Access Token (for operations needing API access)
GITHUB_PAT=""

# Google service account JSON path (use absolute path outside repo)
GCP_CREDS_PATH="~/.config/ctas7-gs-credentials.json"

# Google Sheets name or ID
SHEETS_NAME="CTAS7 QA Results"

# Google Sheets creds path if different
SHEETS_CREDS_PATH="$GCP_CREDS_PATH"

# Other tokens
DOCKER_REGISTRY_TOKEN=""

# Path to secrets drop zone (relative to repo)
SECRETS_DROP_DIR="./secrets/drop"
